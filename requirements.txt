# --- SYSTEM DEPENDENCIES (Install these first) ---
# Ubuntu/Debian: sudo apt install iverilog yosys gtkwave
# Fedora:        sudo dnf install iverilog yosys gtkwave
# Arch Linux:    sudo pacman -S iverilog yosys gtkwave
# macOS:         brew install icarus-verilog yosys gtkwave

# --- PYTHON LIBRARIES ---
rich>=13.0.0
textual>=0.40.0
click>=8.0.0
pexpect>=4.8.0
tabulate>=0.9.0
pandas>=2.0.0
